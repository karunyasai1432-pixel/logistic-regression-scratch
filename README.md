# logistic-regression-scratch
Logistic regression built from scratch using NumPy — implements sigmoid, log-loss cost function, gradient descent, and prediction for binary classification, applied to a student admissions dataset.
